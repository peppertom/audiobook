# Worker-specific dependencies (TTS + PyTorch)
# Includes all base requirements plus ML dependencies
-r requirements.txt
# Pin torch <2.9 to avoid torchcodec requirement (no aarch64 wheels)
torch>=2.4.0,<2.9.0
torchaudio>=2.4.0,<2.9.0
transformers>=4.57.0,<5.0.0
coqui-tts>=0.27.5
pydub==0.25.1
